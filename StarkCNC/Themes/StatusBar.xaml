<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:StarkCNC.Controls">
    <Style TargetType="{x:Type local:StatusBar}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:StatusBar}">
                    <Grid ColumnDefinitions="4*,*">
                        <Grid
                            Grid.Column="0"
                            ColumnDefinitions="6*,*">
                            <Label
                                Grid.Column="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{Binding Status, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Label
                                x:Name="CurrentTimeLabel"
                                Grid.Column="1"
                                Margin="0,0,10,0"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"/>
                        </Grid>
                        <Button
                            x:Name="HistoryButton"
                            Grid.Column="1"
                            HorizontalAlignment="Stretch"
                            Content="История"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>