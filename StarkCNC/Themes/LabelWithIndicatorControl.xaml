<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:StarkCNC.Controls">
    <Style TargetType="{x:Type local:LabelWithIndicatorControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:LabelWithIndicatorControl}">
                    <Grid ColumnDefinitions="*, Auto">
                        <Label Grid.Column="0" Content="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}"/>
                        <Border
                            Grid.Column="1"
                            Margin="10,5,5,5"
                            CornerRadius="5"
                            HorizontalAlignment="Left">
                            <Rectangle x:Name="Indicator" Width="12" Fill="Red"/>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsActive" Value="True">
                            <Setter TargetName="Indicator" Property="Fill" Value="Green"/>
                        </Trigger>
                        <Trigger Property="IsActive" Value="False">
                            <Setter TargetName="Indicator" Property="Fill" Value="Red"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>