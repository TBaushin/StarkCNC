<UserControl x:Class="StarkCNC.Controls.PersonPicture"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StarkCNC.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Ellipse
            x:Name="PictureEllipse"
            Grid.RowSpan="3"
            Grid.ColumnSpan="3"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Stretch="Uniform"
            Stroke="Black"
            StrokeThickness="1"
            ToolTip="{Binding DisplayName, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Ellipse.Fill>
                <ImageBrush ImageSource="{Binding PictureSource, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </Ellipse.Fill>
        </Ellipse>
        <Ellipse
            Grid.RowSpan="3"
            Grid.ColumnSpan="3"
            Panel.ZIndex="-1"
            Fill="{Binding Background, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Stroke="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=UserControl}}"
            StrokeThickness="{Binding StrokeThickness, RelativeSource={RelativeSource AncestorType=UserControl}}"
            ToolTip="{Binding DisplayName, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <TextBlock
            x:Name="InitialsTextBlock"
            Grid.RowSpan="3"
            Grid.ColumnSpan="3"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontFamily="{Binding FontFamily, RelativeSource={RelativeSource AncestorType=UserControl}}"
            FontSize="36"
            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Text="{Binding DisplayName, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Visibility="Collapsed"/>
        <Ellipse
            x:Name="StatusEllipse"
            Grid.Row="2"
            Grid.Column="2"
            MinWidth="5"
            MinHeight="5"
            Margin="0,0,3,3"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            ToolTip="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <Ellipse.Fill>
                <ImageBrush ImageSource="{Binding StatusPictureSource, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            </Ellipse.Fill>
        </Ellipse>
    </Grid>

</UserControl>
