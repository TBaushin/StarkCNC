<UserControl x:Class="StarkCNC.Controls.BorderedContentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StarkCNC.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             MinHeight="80">
    <Border
        Margin="8,4,8,4"
        MinHeight="80"
        CornerRadius="4"
        Background="{DynamicResource CardBackgroundFillColorDefaultBrush}">
        <Grid
            x:Name="BorderedGrid"
            Margin="20,10,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label
                Grid.Column="0"
                VerticalAlignment="Center"
                Content="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
            <ContentControl
                Grid.Column="1"
                VerticalAlignment="Center"
                Content="{Binding BorderedContent, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>
    </Border>
</UserControl>
